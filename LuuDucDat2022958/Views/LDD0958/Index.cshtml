@model IEnumerable<LuuDucDat2022958.Models.LDD0958>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LDDName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LDDGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LDDName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LDDGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LDDid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LDDid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LDDid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
